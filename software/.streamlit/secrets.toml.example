# Copy to secrets.toml for local development, or paste into Streamlit Cloud → App settings → Secrets.

app_password = "choose-a-strong-password"

# Optional overrides (defaults shown)
# refresh_timezone = "America/New_York"
# refresh_hour = 15
# refresh_minute = 0
